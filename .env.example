# MongoDB connection (choose one approach):

# Option 1: Direct URI (recommended for Docker Compose / local MongoDB)
MONGODB_URI=mongodb://localhost:27017/game_db

# Option 2: Atlas credentials (used if MONGODB_URI is not set)
# DB_HOST=your-cluster.mongodb.net
# DB_USERNAME=your_username
# DB_PASSWORD=your_password
